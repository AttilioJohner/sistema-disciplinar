{
  "name": "sistema-disciplinar",
  "version": "1.0.0",
  "scripts": {
    "dev": "http-server public -p 5173 -c-1",
    "build": "node scripts/build-public.js",
    "clean": "rimraf public && mkdir public"
  },
  "private": "true",
  "devDependencies": {
    "http-server": "^14.1.1",
    "rimraf": "^6.0.1"
  }
}
